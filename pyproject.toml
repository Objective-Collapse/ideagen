[project]
name = "ideagen"
version = "0.1.0"
description = "Application to generate random business idea to test Vercel link and call to OpenApi"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.3",
    "openai>=2.38.0",
    "uvicorn>=0.48.0",
]
